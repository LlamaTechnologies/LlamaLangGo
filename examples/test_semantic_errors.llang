/*
* ERROR redefinition of x
*/
func main(argc: int, argv: []char) -> int32 {
    x : int32;
    x : uint8;

    return x;
}

/*
* ERROR left reference to b doesn't exists
*/
func main(argv: []char) -> uint16 {
    x : int32;
    b = x;

    return y;
}

/*
* ERROR right reference to b doesn't exists
*/
func main(argv: []char) -> uint16 {
    x : int32;
    y : int32 = b;

    return y;
}
